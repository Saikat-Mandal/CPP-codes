#include<iostream>
#include<list>
    using namespace std;
int main()
{
    list<int> l1;
    list<int> l2(7);
    // same as vector 
    // l1.pop_back();
    // l1.pop_front();
    // l1.remove(element);
    return 0;
}